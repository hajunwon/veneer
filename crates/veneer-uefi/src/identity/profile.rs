//! Synthetic-PC profile schema. The definitions live in the shared
//! `veneer-profile` crate so the host tools see the exact same layout;
//! re-exported here under the historical `identity::profile` path.

pub use veneer_profile::*;
